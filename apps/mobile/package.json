{
  "name": "video-agent-mobile",
  "version": "0.4.0-prototype",
  "private": true,
  "scripts": {
    "start": "react-native start",
    "ios": "react-native run-ios",
    "android": "react-native run-android",
    "pods:ios": "cd ios && pod install",
    "build:ios": "cd ios && xcodebuild -workspace VideoAgentMobile.xcworkspace -scheme VideoAgentMobile -configuration Debug -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO build",
    "codegen:android": "cd android && sh ./gradlew :app:generateCodegenArtifactsFromSchema --no-daemon",
    "build:android": "cd android && sh ./gradlew :app:assembleDebug -PreactNativeArchitectures=x86_64 --no-daemon",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "^19.2.3",
    "react-native": "0.87.0"
  },
  "devDependencies": {
    "@react-native-community/cli": "latest",
    "@react-native/typescript-config": "0.87.0",
    "@types/react": "^19.2.0",
    "typescript": "^5.9.0"
  },
  "codegenConfig": {
    "name": "NativeVideoHostSpec",
    "type": "modules",
    "jsSrcsDir": "specs",
    "android": { "javaPackageName": "com.videoagent.mobile" }
  }
}
